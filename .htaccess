DirectoryIndex index.php index.html

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    RewriteRule ^$ index.php [L]
</IfModule>

# Block direct access to PHP includes (not meant to be hit in the browser)
<IfModule mod_authz_core.c>
    RewriteRule ^includes/ - [F,L]
</IfModule>
